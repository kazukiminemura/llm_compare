[project]
name = "llm-compare"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numba",
    "openvino-genai",
    "optimum-intel[openvino]>=2.0.0",
    "torch",
    "transformers>=4.45,<5",
]
